Project 3 — Vaultly / AWS / Terraform — Decisions

KEY CONFIG FACTS:

- App port: 8080
- App routes: /healthz, /readyz, /api/v1/secrets/{path}
- Vault address from pod: http://vault-active.vault.svc:8200
- Vault env prefix: VAULTLY_
- VAULTLY_VAULT_AUTH_METHOD = kubernetes
- VAULTLY_VAULT_K8S_ROLE = vaultly
- serviceAccountName: vaultly-sa
- App KV path = URL path directly. Policy restricts to vaultly/* so all API calls must use /api/v1/secrets/vaultly/...
- Root token: hvs.xxxxxxxxxxxxx (still needed for admin ops, NOT used by app)
- Unseal keys: 5 shares, threshold 3 (saved by user)
- Control-plane taint removed (re-do if cluster recreated)
- vaultly-secrets k8s secret still exists (can be deleted, no longer referenced)
- VAULTLY_API_BEARER_TOKEN still hardcoded in YAML (move to secret later)

PROJECT 3 — DECISIONS
=====================

DECIDED (cloud-agnostic, safe to build on)
------------------------------------------

[x] App is Vaultly (Python/FastAPI, KV v2)
    Cloned, run locally, tested. PUT/GET verified.
    Talks to Vault via VAULT_ADDR + VAULT_TOKEN env vars.

[x] Vault storage is Raft (HA, 3 replicas)
    Running on kind. 3 pods, self-contained, no external dep.

[x] Local practice is kind (1 CP + 2 workers)
    Cluster "vaultly" running, 3 nodes Ready.

[x] kind config lives in repo at infra/kind/kind-config.yaml
    Version-controlled, in use.

[x] Token strategy — root token (kind, local only)
    Root token saved, used in vaultly-secrets k8s secret.
    ServiceAccount/AppRole for prod is NOT done yet.

[ ] IaC tool is Terraform
    Decided, but no .tf files written yet.

[ ] CI/CD tool is GitHub Actions
    Decided, but no workflow YAML written yet.

[ ] App-level CI scope is lint + test + build only
    Decided, not implemented.

[ ] No secrets in pipeline env vars
    Decided, not implemented (no pipeline exists yet).


NOT DECIDED (hosting-dependent, may change if Azure)
-----------------------------------------------------

[ ] Cloud provider (AWS or Azure)
[ ] Final Kubernetes deliverable (EKS or AKS)
[ ] Auto-unseal mechanism (AWS KMS or Azure Key Vault)
[ ] Cloud auth from CI (OIDC to AWS STS or Azure AD)
[ ] VPC/VNet layout and Terraform module (aws vs azurerm)
[ ] Security posture tooling (GuardDuty or Defender for Cloud)
[ ] Audit logging (CloudTrail to S3 or Activity Log to Storage Account)
--------------------------------------------------------------------------
--------------------------------------------------------------------------

KEY LESSONS / NOTES:

- App uses VAULTLY_ prefix on ALL env vars
- VAULTLY_VAULT_ADDR must be http://vault-active.vault.svc:8200 (not vault.vault.svc)
- App KV path = URL path. Policy path secret/data/vaultly/* means API calls must target /api/v1/secrets/vaultly/...
- Heredocs break in this terminal — use echo lines to build files instead
- kind get kubeconfig --name vaultly > ~/.kube/config fixes kubectl connection
- User gets burned out easily — keep chunks short, one thing at a time
- User prefers objective/educational tone over direct "you" speak
- User tends to typo vaultly vs vaulty — double-check namespace names
- WSL2: VS Code auto-forwards Vault 8200, NOT app port 8080